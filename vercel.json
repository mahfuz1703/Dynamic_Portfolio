{
    "builds": [{
        "src": "Dynamic_Portfolio/wsgi.py",
        "use": "@vercel/wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "Dynamic_Portfolio/wsgi.py"
        }
    ]
}